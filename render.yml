services:
  type: web
  name: bibliotecavirtual-aprofunda
  env: node
  plan: free
  buildCommand: npm run build
  startCommand: node dist/index.js
  envVars:
  - key: NODE_ENV
    value: production
  - key: PORT
    value: 10000
